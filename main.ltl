// ltl-life/main.ltl
//
// Conway's Game of Life — infinite plane via hashlife.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-life v0.1.0")
    io::println("Conway's Game of Life — infinite plane via hashlife.")
    0
}
