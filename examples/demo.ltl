// examples/demo.ltl — runnable demo for ltl_life

import std::io
use ltl_life as lib

fn main() {
    io::println("ltl_life demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}